import Home from "@/pages/Home.vue";
import News from "@/pages/News.vue";

const routes = [
    {
        path: '/',
        component: Home,
    },
    {
        path: '/news',
        component: News,
    }
]

export default routes;
